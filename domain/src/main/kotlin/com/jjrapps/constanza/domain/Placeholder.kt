package com.jjrapps.constanza.domain

/**
 * Scaffolding placeholder. Real model types (Habit, Schedule, Entry, dueOn, rollupDay,
 * StreakCalculator, ComplianceCalculator, ...) land in work unit 2a/2b (design.md §14).
 *
 * This module applies kotlin("jvm") only: android.* and androidx.* cannot resolve here. Time
 * enters this module as an explicit parameter, never by reading the clock directly (design.md §4).
 */
internal object Placeholder
